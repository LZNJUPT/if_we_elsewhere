# 隐私设计（PRIVACY）

> 一句话：**你的数据属于你**。所有重活都在本地跑；只有一步（生成回复）会把
> 明确划定的脱敏文本发给你自己配置的 LLM。

## 数据流总览

```
┌────────────────────────── 你的电脑（本地） ──────────────────────────┐
│                                                                      │
│  原始 JSONL（只读，永不修改）                                          │
│      │  Phase1: 去噪 + 脱敏（手机号/地址/证件号/卡号 → 占位符）          │
│      ▼                                                               │
│  data/ifwe_v1.db   ← messages(含 content_orig 仅本地) / sessions /     │
│      │               events / facts / relationship_state / …          │
│      │                                                               │
│      ├─► analyze：事件抽取、记忆、关系状态、转折点、persona             │
│      │        （LLM 路径：只发送脱敏文本；离线路径：完全本地）           │
│      ▼                                                               │
│  DialEngine 对话推演：只写 sim_* 隔离命名空间；主库只读                 │
│      │                                                               │
│      │  ⚠ 唯一出网点：生成回复时，发送以下内容给你配置的 LLM API：       │
│      │    - persona 档案（你/分析生成的人格描述）                       │
│      │    - 相关记忆（content_clean 派生结论）                          │
│      │    - 最近对话缓冲（脱敏文本）                                   │
│      │    - 你的输入与改写场景卡                                       │
│      ▼                                                               │
│  你的 LLM API（DeepSeek / 其他 OpenAI 兼容端点 / 本地模型）             │
└──────────────────────────────────────────────────────────────────────┘
```

## 具体保证

| 层面 | 做法 |
|---|---|
| 原始记录 | 导入后原文件保持只读；库内 `content_orig` 仅存于你本机数据库，不参与任何外发路径 |
| 脱敏 | 中档脱敏在**入库前**执行；`content_clean` 是分析与 LLM 的唯一文本来源 |
| 模拟隔离 | 推演写入 `sim_*` 前缀表 + 独立分支表；主库在推演过程中零写入（selftest 有自动化验证） |
| 反事实冻结 | IF 线的分歧点之后，主库真实记忆被时间截断，模型看不到「被你改写掉的未来」 |
| 密钥 | 环境变量（默认 `LLM_API_KEY`）优先；界面「设置」面板填写的密钥存进 **Windows 凭据管理器**（服务名 `IfWe`，账号为 `llm.api_key_env` 的值），不可用时回退 `data/.secret_llm_key`（**当前 Windows 用户 DPAPI 加密**）。`config.yaml`、日志、任何 API 响应都不会出现明文密钥（`GET /api/settings` 只回 `sk-***abc` 形式的提示片段） |
| Web 导入 | 上传的文件仅落本机临时目录 `data/tmp_import/`；上传 → 预览 → 提交链路结束（含失败）**立即删除**；预览只返回统计与脱敏样例，不回显原文；响应不含服务器路径 |
| 多好友隔离 | 每个好友一个独立数据目录 `data/profiles/<id>/`（库 / persona / 缓存 / 对话线全隔离）；切换好友即切换整个数据上下文，互不可见；老数据迁移前整目录备份到 `data_backup_<日期>/`（已 gitignore） |
| 仓库卫生 | `data/`、`data_demo/`、`config.yaml`、`data_backup_*/` 永远 gitignore；示例数据为纯虚构 |

## Web 导入向导的数据流（v0.2，v0.3 扩展为多份来源）

左侧栏「导入记录」的上传链路与 CLI 导入使用同一套本地流水线：

1. **上传**：文件写入本机 `data/tmp_import/<随机ID>`（该目录在 `data/`
   内，已被 gitignore）；接受 `.jsonl / .json / .csv / .txt / .md / .log / .docx`
   （`.pdf` 只做识别与转存引导，不解析），单份上限 50MB、单批最多 12 份，
   同时仅允许一个导入任务（文件锁）；
2. **预览**：在本机逐份完成格式识别、统计与脱敏样例生成，并试算合并结果；
   响应只含统计、每方前 3 条**脱敏**样例——不含原文，不含服务器路径；
3. **提交**：把本批来源归档到 `data/profiles/<id>/sources/` 并登记进 `sources.json`，
   然后从**全部已登记来源**重建库 + 质量门禁；
4. **删除**：预览失败或提交结束（无论成败）后，**临时文件与锁立即删除**；
   超过 1 小时的遗留任务会被自动回收。注意：**源存档是有意保留的**
   （用于「分多次导入后自动合并」），见下一节。

全程发生在你的电脑上；本项目没有任何服务器，不产生任何远端上传。

## 源文件存档与媒体（v0.3，需要你知情）

v0.3 起导入有两处**有意保留**的本地数据，都是为了让「同一段对话散在不同应用里」
这个真实场景可用：

**1. 源文件存档**：`data/profiles/<id>/sources/<内容哈希前16位>.<ext>`
——库是由「全部已登记来源」重建出来的结果，所以源文件必须留档，否则第二次导入时
无法把第一次的来源一起重建。登记表为 `data/profiles/<id>/sources.json`
（只记文件名、格式、逐源 A/B 映射与条数，不含内容）。
界面「导入记录 → 已导入的来源」可查看并**单独移除**任一份（移除即删存档并重建库）；
命令行可用 `--no-archive` 完全不登记。整份数据随好友目录，删除好友时一并删除。

**2. 媒体库**：`data/profiles/<id>/media/<内容哈希前16位><ext>`

> ⚠️ **媒体不做脱敏，这是与聊天文本最大的不同。**
> 聊天文字入库前会把手机号 / 身份证 / 银行卡 / 车牌 / 邮箱 / 地址替换为占位符；
> 但**图片是原始二进制，没有任何处理**。一张身份证截图、一张快递单照片、
> 一张含地址的聊天截图，导入后就是原样躺在上面那个目录里。
>
> 配套保证：
> - 媒体**不参与任何分析，也不会被发送给 LLM**（只有脱敏文本会外发）；
> - 媒体按内容哈希去重，不重复占空间；删除媒体只影响界面显示，不动聊天记录；
> - 导入前请自行确认图片不含你不希望留在本机的敏感信息。

## 密钥与数据目录（v0.3 桌面化）

**密钥**（顺序即优先级）：

1. 环境变量（`llm.api_key_env`，默认 `LLM_API_KEY`）——方便 CI 或临时覆盖，不落盘；
2. Windows 凭据管理器（服务名 `IfWe`，账号 = `llm.api_key_env` 的值）——
   界面「设置」保存的位置，可在「凭据管理器 → Windows 凭据」里看到与撤销；
3. `data/.secret_llm_key` —— 凭据管理器不可用时的回退，内容为 DPAPI 密文，
   只有当前 Windows 用户能解密（换用户/换机器都解不开）。

清除密钥：界面「设置 → 清除密钥」或 `python run.py key clear`（两个位置一起清）。

**数据目录**（多好友）：

```
data/
├── profiles.json          # 好友注册表（id / 名字 / 相对数据目录 / 上次使用时间）
├── profiles/<id>/         # 每个好友一整套：ifwe_v1.db、persona/、cache/、profile.yaml
│   ├── sources/           # 已导入来源的存档（v0.3；库由它们重建，界面可单独移除）
│   ├── sources.json       # 来源登记表（文件名 / 格式 / 逐源 A/B 映射 / 条数）
│   └── media/             # 媒体库：表情包与图片（v0.3；不做脱敏，不进 LLM）
├── tmp_import/            # 导入向导的临时落盘（链路结束即删）
└── .secret_llm_key        # 仅在回退路径下存在（DPAPI 密文）
```

接口只回相对路径（如 `data/profiles/ada`），不回传本机绝对路径。

## 启动器的本地痕迹（desktop.py / IfWe.exe）

- 单实例锁：`data/.ifwe.lock`（记录 pid + 端口 + 启动时间），正常退出即删除；
  异常退出留下的锁会在下次启动时清理，并尝试结束那个「看起来是我们自己」的孤儿进程；
- 启动日志：`data/logs/desktop.log`（双击启动没有控制台，启动信息与异常堆栈写在这里，
  方便反馈问题时附带；超 512KB 自动重建；只含本机路径与状态行，不含聊天内容）；
- 只监听 `127.0.0.1`，不对局域网/公网开放；关窗即停止服务；
- 打包运行时 `data/` 仍在 exe 同级目录，不写注册表、不写系统目录。

## 隐私扫描门禁（本项目的自我要求）

`scripts/check_privacy.py` 是发布门禁：用红线词表扫描仓库内全部文本文件，
任何命中即非零退出。它解决的问题是——在把「私密项目」改造成「公开项目」的
过程中，防止任何人的真实昵称、地点、日期锚点、事件情节混进代码或文档。

```bash
python scripts/check_privacy.py                 # 发布前/提交前运行
python scripts/check_privacy.py --extra my_words.txt   # 追加你自己的词表
```

建议挂到 pre-commit 或 CI。本项目自己的发布流程就是用它在提交前逐轮清零的。

## 你需要知道的残留风险

- LLM 提供商会看到发送给它的脱敏文本（受其隐私政策约束；用本地模型可完全避免）。
- 库内 `content_orig` 保留了原始文本（这是「真实记录回放」功能的基础）；请像
  保护数据库文件一样保护你的 `data/` 目录。
- 表情包复现功能会把表情图渲染到界面，属于本地行为，不外发。

有任何隐私相关的疑问或建议，欢迎提 issue。
